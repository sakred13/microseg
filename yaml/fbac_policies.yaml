apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-drone1-to-drone3
spec:
  podSelector:
    matchLabels:
      app: droneservice
  policyTypes:
    - Ingress
  ingress:
    - from:
      - podSelector:
          matchLabels:
            app: droneservice
            name: drone1service1
      - podSelector:
          matchLabels:
            app: droneservice
            name: drone3service1
